// Top-level build file. If Android Studio offers to upgrade the Android
// Gradle Plugin or Kotlin, accept it — the versions below are a known-good
// baseline and will sync cleanly on a current Android Studio.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
