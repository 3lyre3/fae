# Foxfire is a thin WebView shell — the default rules are enough.
# Keep JS interface hooks if you ever add @JavascriptInterface members:
# -keepclassmembers class * { @android.webkit.JavascriptInterface <methods>; }
